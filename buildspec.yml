version: 0.2
phases:
  build:
    commands:
      - echo "Building Nuvraverse application..."
      - echo "Build completed on $(date)"
artifacts:
  files:
    - index.html
    - appspec.yml
    - scripts/**/*
  discard-paths: no
